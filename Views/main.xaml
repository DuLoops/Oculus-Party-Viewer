<UserControl x:Class="Test.Views.main"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Test.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#cec2ae">
        <ToolBarTray DockPanel.Dock="Top" Margin="0,0,0,412" Background="#264c5e">
            <ToolBar>
                <Button Content="Edit" FontSize="15" Margin="0,0,5, 0" BorderBrush="Gray" Padding="5"/>
                <Button Content="Parties" FontSize="15"  BorderBrush="Gray" Padding="5" />
            </ToolBar>
        </ToolBarTray>
        <StackPanel Orientation="Horizontal" Margin="10,53,10,10" >
            <Rectangle Width="150" Fill="#7FA34C50">
                
            </Rectangle>
        </StackPanel>
    </Grid>
</UserControl>
